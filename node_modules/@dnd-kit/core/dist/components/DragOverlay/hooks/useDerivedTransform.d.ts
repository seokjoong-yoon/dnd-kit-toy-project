import { Transform } from '@dnd-kit/utilities';
import type { ViewRect } from '../../../types';
export declare function useDerivedTransform(transform: Transform, rect: ViewRect | null, overlayNode: HTMLElement | null): Transform | undefined;
