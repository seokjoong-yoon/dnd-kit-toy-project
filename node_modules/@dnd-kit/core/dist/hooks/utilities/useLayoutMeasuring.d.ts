import type { DroppableContainers, LayoutRectMap } from '../../store/types';
interface Arguments {
    dragging: boolean;
    dependencies: any[];
    config: Partial<LayoutMeasuring> | undefined;
}
export declare enum LayoutMeasuringStrategy {
    Always = 0,
    BeforeDragging = 1,
    WhileDragging = 2
}
export declare enum LayoutMeasuringFrequency {
    Optimized = "optimized"
}
export interface LayoutMeasuring {
    strategy: LayoutMeasuringStrategy;
    frequency: LayoutMeasuringFrequency | number;
}
export declare function useLayoutMeasuring(containers: DroppableContainers, { dragging, dependencies, config }: Arguments): {
    layoutRectMap: LayoutRectMap;
    recomputeLayouts: () => void;
    willRecomputeLayouts: boolean;
};
export {};
